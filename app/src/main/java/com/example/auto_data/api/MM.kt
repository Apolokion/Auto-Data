package com.example.auto_data.api

/*

import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.padding
import androidx.compose.foundation.lazy.LazyColumn
import androidx.compose.foundation.lazy.items
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.unit.dp
import androidx.lifecycle.viewmodel.compose.viewModel
import com.example.auto_data.api.Brand


Surface(color = MaterialTheme.colorScheme.background) {
    BrandListScreen()
}


@Composable
fun BrandListScreen(viewModel: CompareScreenViewModel = viewModel()) {
    val brands = viewModel.brands

    LazyColumn {
        items(brands) { brand ->
            BrandItem(brand)
        }
    }
}

@Composable
fun BrandItem(brand: Brand) {
    Column(modifier = Modifier.padding(16.dp)) {
        Text(text = brand.name, style = MaterialTheme.typography.titleSmall)
    }
}
 */